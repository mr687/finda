const help = require('./help')
const find = require('./find')
const stop = require('./stop')
const simi = require('./simi')
const admin = require('./admin')

module.exports = {
  help,
  find,
  stop,
  simi,
  admin
}